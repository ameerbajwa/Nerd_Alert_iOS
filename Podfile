# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Nerd_Alert_iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Nerd_Alert_iOS
  pod 'ObjectMapper', '~> 3.4'
  pod 'Marshal', '~> 1.2.7'

  target 'Nerd_Alert_iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Nerd_Alert_iOSUITests' do
    # Pods for testing
  end

end
